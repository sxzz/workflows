name: autofix.ci

on:
  push:
    branches: [main]

  pull_request:

  workflow_call:
    inputs:
      command:
        required: false
        type: string
        default: pnpm run lint --fix
      os:
        required: false
        type: string
        default: ubuntu-slim

jobs:
  autofix:
    runs-on: ${{ inputs.os || 'ubuntu-slim' }}
    permissions:
      contents: read

    steps:
      - name: Setup JS
        uses: sxzz/workflows/setup-js@v1
        with:
          no-frozen-lockfile: true

      - name: Fix
        run: ${{ inputs.command }}
        if: ${{ inputs.command != '' }}

      - name: Commit
        uses: autofix-ci/action@7a166d7532b277f34e16238930461bf77f9d7ed8 # v1.3.3
