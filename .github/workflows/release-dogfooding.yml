name: Release

on:
  push:
    tags:
      - 'v*'

jobs:
  release:
    uses: sxzz/workflows/.github/workflows/release.yml@next
    with:
      publish: false
    permissions:
      contents: write
      id-token: write
